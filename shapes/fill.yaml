import: 
    - ../functions/aastep.yaml

styles:
    shapes-fill:
        mix: [functions-aastep]
        shaders:
            blocks:
                global: |
                    float fill (in float size, in float x) {
                        return 1.-aastep(size, x);
                    }
