# Patricio Gonzalez Vivo - 2016

styles:
    patterns-lines-dots:
        base: lines
        shaders:
            defines:
                dotSize: .05
            blocks:
                color: |
                    vec2 st = fract(v_texcoord.xy);
                    st -= .5;
                    color.a = 1.- step(dotSize, dot(st,st)*2.);